"""Module providing a factory class to create a Taxon class."""

from ..abstract_factory import AbstractFactory
from ...taxons import Taxon

class TaxonFactory(AbstractFactory):
    """Factory class to create a Taxon class."""

    